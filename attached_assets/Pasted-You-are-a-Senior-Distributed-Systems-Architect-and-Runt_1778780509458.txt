You are a Senior Distributed Systems Architect and Runtime Infrastructure Engineer.

Your task is to DEEP SCAN the entire backend runtime architecture and FIX the "Runtime Identity Crisis" bug.

━━━━━━━━━━━━━━━━━━━━
PRIMARY CRITICAL BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: P9 — Runtime Identity Crisis

Current problem:
The system has 3 separate runtime/process managers fighting each other.

Detected runtime systems:
1. server/api/runtime.routes.ts
2. server/preview/runtime/runtime.service.ts
3. server/agents/tools/categories/server-lifecycle-tools.ts

All 3:
- spawn processes
- track ports
- manage runtime state
- manage logs
- manage restart/stop
- maintain their own Maps

This creates:
- preview 502 errors
- runtime desync
- orphan processes
- wrong process states
- broken preview routing
- unstable agent execution
- memory leaks
- zombie runtimes
- duplicate server instances
- port collisions

━━━━━━━━━━━━━━━━━━━━
YOUR TASK
━━━━━━━━━━━━━━━━━━━━

Perform a FULL deep scan and identify:

1. All runtime managers
2. All process maps
3. All child_process.spawn usages
4. All port allocation systems
5. All preview-port mappings
6. All runtime state stores
7. All runtime lifecycle handlers
8. All restart/stop handlers
9. All runtime event emitters
10. All preview integrations
11. All runtime SSE/WebSocket dependencies
12. All process cleanup logic
13. All runtime-related imports
14. All circular runtime dependencies
15. All duplicated runtime logic

━━━━━━━━━━━━━━━━━━━━
THEN FIX THE ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create ONE unified runtime architecture.

Target architecture:

server/
└── infrastructure/
    └── runtime/
        ├── process-registry.ts
        ├── runtime-manager.ts
        ├── port-manager.ts
        ├── runtime-events.ts
        ├── runtime-types.ts
        └── runtime-health.ts

━━━━━━━━━━━━━━━━━━━━
MANDATORY RULES
━━━━━━━━━━━━━━━━━━━━

1. ONLY ONE SOURCE OF TRUTH
There must be ONE central process registry.

2. NO DUPLICATE MAPS
Remove all duplicated:
- process Maps
- port Maps
- runtime state Maps

3. ALL SYSTEMS MUST USE SAME RUNTIME
Preview, agent tools, API routes, and runtime services must all use:
- processRegistry
- runtimeManager

4. PREVIEW MUST READ FROM REGISTRY
Preview proxy must dynamically resolve port from central registry.

5. NO DIRECT CHILD PROCESS MANAGEMENT
No file should directly manage child processes except runtime-manager.ts

6. ADD SAFE CLEANUP
Add:
- process cleanup
- orphan cleanup
- SIGTERM handling
- SIGINT handling
- process exit cleanup

7. ADD HEALTH TRACKING
Track:
- pid
- port
- startedAt
- status
- memory
- uptime
- restart count

8. NO MEMORY LEAKS
Ensure:
- listeners removed
- intervals cleaned
- processes deregistered
- ports released

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CODING RULES
━━━━━━━━━━━━━━━━━━━━

- Use LOW coupling
- Use HIGH cohesion
- Clear folder structure
- Clear responsibility separation
- No god classes
- No circular imports
- Strong TypeScript typing
- Reusable services
- Minimal shared mutable state

━━━━━━━━━━━━━━━━━━━━
FILE RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:
- Keep EVERY file UNDER 250 lines
- Split responsibilities cleanly
- Do not create massive files
- Avoid deeply nested logic
- Extract helpers/services when needed

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Runtime architecture audit
2. Runtime conflict report
3. Process map conflict report
4. Runtime dependency graph
5. Runtime lifecycle flow
6. Exact root causes
7. Files causing conflicts
8. Line references
9. Unified runtime architecture
10. Refactor plan
11. New folder structure
12. New runtime service design
13. Exact code changes
14. Migration strategy
15. Cleanup strategy
16. Restart recovery strategy
17. Memory leak prevention strategy
18. Final production-grade runtime architecture

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite unrelated systems
- break existing APIs unnecessarily
- create giant files
- introduce tight coupling
- duplicate runtime state

ONLY:
- unify runtime management
- stabilize runtime lifecycle
- centralize process control
- fix preview/runtime desync
- remove runtime conflicts
- make architecture production-grade

Use evidence-based analysis only.
Mention exact files and exact conflicting logic.