You are a Principal Backend Runtime Engineer and Fault-Tolerant Systems Architect.

Your task is to DEEP SCAN the entire backend runtime architecture and FIX the following critical production issue:

━━━━━━━━━━━━━━━━━━━━
CRITICAL BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: P8

Problem:
The system stores running process state ONLY in memory using Maps/Sets.

Examples:
- runningServers Map
- processes Map
- usedPorts Set
- runtime state caches
- child process registries

Current Result:
If the backend/server restarts:
- all runtime state is lost
- preview breaks
- orphan processes remain alive
- ports remain occupied
- UI shows wrong status
- runtime cannot reconnect
- project state becomes inconsistent

This causes:
- broken previews
- zombie processes
- invalid runtime states
- failed restarts
- unstable autonomous execution

━━━━━━━━━━━━━━━━━━━━
YOUR OBJECTIVES
━━━━━━━━━━━━━━━━━━━━

1. Deep scan runtime architecture
2. Find ALL in-memory process registries
3. Find ALL runtime state maps/sets
4. Find ALL child process tracking logic
5. Find ALL port allocation systems
6. Find ALL preview-runtime dependencies
7. Find ALL restart failure points
8. Find ALL runtime desync issues
9. Find ALL orphan-process risks
10. Build a persistent runtime architecture

━━━━━━━━━━━━━━━━━━━━
FOCUS DIRECTORIES
━━━━━━━━━━━━━━━━━━━━

Scan deeply:
- server/api/
- server/runtime/
- server/preview/
- server/infrastructure/
- server/chat/
- server/agents/
- server/services/

Especially:
- runtime.routes.ts
- runtime.service.ts
- preview-proxy.ts
- server-lifecycle-tools.ts
- project-runner.service.ts
- tool-loop.agent.ts
- process maps
- child process managers

━━━━━━━━━━━━━━━━━━━━
DETECT
━━━━━━━━━━━━━━━━━━━━

Find:
- Map<number, ChildProcess>
- Map<string, Process>
- Set<number>
- in-memory runtime caches
- active process registries
- orphan child processes
- detached spawn calls
- missing persistence
- missing PID recovery
- missing startup reconciliation
- missing shutdown cleanup
- missing runtime restore
- missing health checks
- missing process synchronization

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIX
━━━━━━━━━━━━━━━━━━━━

Design and implement:

✅ Unified Process Registry
✅ Persistent Runtime State
✅ Process Recovery System
✅ Startup Reconciliation
✅ Zombie Cleanup
✅ Port Recovery
✅ Runtime Health Checks
✅ Crash Recovery
✅ Runtime Reconnect Logic
✅ Preview Rebinding

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create:

server/infrastructure/process/
  ├── process-registry.ts
  ├── process-persistence.ts
  ├── process-recovery.ts
  ├── process-health.ts
  ├── process-types.ts

Requirements:
- clean architecture
- low coupling
- high cohesion
- single responsibility
- modular services
- production-grade structure
- typed runtime state
- centralized runtime ownership

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

1. Every file MUST stay under 250 lines
2. No god classes
3. No giant services
4. No circular dependencies
5. No duplicated runtime maps
6. One source of truth only
7. Avoid tightly coupled systems
8. Use dependency injection patterns
9. Use explicit interfaces/types
10. Keep preview/runtime/tool systems decoupled

━━━━━━━━━━━━━━━━━━━━
PERSISTENCE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Persist:
- projectId
- pid
- port
- command
- cwd
- startedAt
- status
- restartCount
- lastHeartbeat

Storage:
- PostgreSQL OR lightweight JSON runtime snapshots
- Must survive backend restart

━━━━━━━━━━━━━━━━━━━━
STARTUP RECOVERY FLOW
━━━━━━━━━━━━━━━━━━━━

On backend startup:

1. Load persisted runtime state
2. Verify PID existence
3. Verify process health
4. Recover valid processes
5. Cleanup dead/zombie entries
6. Rebind preview ports
7. Restore runtime registry
8. Restore runtime status
9. Emit recovery lifecycle events

━━━━━━━━━━━━━━━━━━━━
HEALTH SYSTEM
━━━━━━━━━━━━━━━━━━━━

Implement:
- periodic heartbeat
- process liveness checks
- crash detection
- auto cleanup
- stale process eviction
- runtime timeout detection

━━━━━━━━━━━━━━━━━━━━
PREVIEW FIX
━━━━━━━━━━━━━━━━━━━━

Preview proxy MUST:
- NEVER use local Maps
- ALWAYS query ProcessRegistry
- ALWAYS resolve live runtime state
- recover after restart automatically

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root Cause Analysis
2. Runtime Failure Map
3. Files causing persistence loss
4. Runtime lifecycle diagram
5. New architecture design
6. Folder structure
7. Exact files to create
8. Exact files to modify
9. Migration strategy
10. Recovery flow
11. Cleanup strategy
12. Health monitoring flow
13. Refactored runtime ownership model
14. Production-safe implementation

Then:
- IMPLEMENT fixes
- REFACTOR architecture
- REMOVE duplicated runtime ownership
- UNIFY process management

DO NOT:
- rewrite unrelated systems
- break current APIs
- create giant files
- mix preview/runtime responsibilities
- leave hidden runtime maps

ONLY:
- stabilize runtime persistence
- fix restart recovery
- unify process ownership
- harden runtime architecture