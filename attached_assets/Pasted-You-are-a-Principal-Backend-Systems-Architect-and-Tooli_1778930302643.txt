You are a Principal Backend Systems Architect and Tooling Infrastructure Engineer.

Your task is to deeply scan the entire codebase and FIX the following architecture problem:

━━━━━━━━━━━━━━━━━━━━
TARGET PROBLEM
━━━━━━━━━━━━━━━━━━━━

T3 — No Unified Tool Registry
Current system has fragmented tools spread across multiple files and execution paths.

Problem symptoms:
- tools are registered inconsistently
- tool execution logic is duplicated
- some tools bypass orchestration
- no single source of truth
- tools cannot be discovered centrally
- capability mapping is fragmented
- tool metadata is inconsistent
- observability is weak
- runtime tools and pipeline tools are disconnected
- tool permissions/security are inconsistent

Current architecture creates:
- execution confusion
- dead tools
- duplicate tools
- unsafe execution paths
- maintenance nightmare
- impossible scaling

━━━━━━━━━━━━━━━━━━━━
MAIN OBJECTIVE
━━━━━━━━━━━━━━━━━━━━

Create a PRODUCTION-GRADE UNIFIED TOOL REGISTRY.

The new architecture must:
- centralize all tools
- standardize tool definitions
- standardize execution flow
- standardize validation
- standardize permissions
- standardize observability
- standardize event emission
- support future tool-calling AI loops
- support autonomous agents
- support planner + executor architecture

━━━━━━━━━━━━━━━━━━━━
SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Deep scan entire codebase and find:

1. all tool definitions
2. all tool execution paths
3. duplicate execution systems
4. fragmented registries
5. bypassed orchestration
6. direct shell calls
7. direct fs calls
8. direct child_process usage
9. direct git usage
10. direct package manager usage
11. hidden tools
12. dead tools
13. fake tools
14. unsafe tools
15. unregistered tools
16. tools not emitting events
17. tools bypassing security
18. tools bypassing sandbox
19. tools bypassing runtime manager
20. tools bypassing process registry

━━━━━━━━━━━━━━━━━━━━
FIND ALL TOOL SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Search especially:
- server/agents/tools/
- server/agents/core/
- server/chat/
- server/runtime/
- server/infrastructure/
- server/services/
- server/api/

Find:
- every execute()
- every spawn()
- every exec()
- every fs.writeFile()
- every git command
- every npm command
- every runtime start/stop
- every shell command
- every process manager

━━━━━━━━━━━━━━━━━━━━
NEW ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create ONE unified system:

server/
└── tools/
    ├── registry/
    │   ├── tool-registry.ts
    │   ├── tool-types.ts
    │   ├── tool-security.ts
    │   ├── tool-validator.ts
    │   └── tool-events.ts
    │
    ├── categories/
    │   ├── file/
    │   ├── shell/
    │   ├── git/
    │   ├── package/
    │   ├── runtime/
    │   ├── browser/
    │   ├── database/
    │   └── agent/
    │
    └── core/
        ├── execute-tool.ts
        ├── tool-context.ts
        ├── tool-result.ts
        └── tool-permissions.ts

━━━━━━━━━━━━━━━━━━━━
STRICT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. SINGLE SOURCE OF TRUTH
There must be ONLY ONE registry.

2. NO DIRECT EXECUTION
No tool may directly:
- spawn child processes
- access fs
- run shell
- mutate runtime
without going through registry execution.

3. TOOL CONTRACT
Every tool MUST implement:

{
  id,
  name,
  category,
  description,
  inputSchema,
  permissions,
  execute(),
}

4. SECURITY LAYER
Every tool execution must pass through:
- permission checks
- sandbox validation
- argument validation
- timeout handling
- audit logging

5. EVENT SYSTEM
Every tool execution must emit:
- tool.start
- tool.success
- tool.error
- tool.output

6. OBSERVABILITY
Track:
- duration
- failures
- retries
- memory usage
- process usage

7. SMALL FILES ONLY
VERY IMPORTANT:
No file may exceed 250 lines.

Split code aggressively into small modules.

8. CLEAN FOLDER STRUCTURE
No god files.
No giant registries.
No circular imports.
No duplicated logic.

━━━━━━━━━━━━━━━━━━━━
FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Refactor:
- fragmented tool systems
- duplicate execution logic
- unsafe shell execution
- direct runtime mutation
- direct fs writes
- direct process spawning

Replace all of them with unified execution.

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Full Tool System Audit
2. All Fragmented Tool Paths
3. Duplicate Tool Execution Map
4. Unsafe Execution Map
5. New Unified Architecture
6. Refactored Folder Structure
7. Migration Plan
8. Exact Files To Create
9. Exact Files To Delete
10. Exact Imports To Replace
11. Security Hardening Plan
12. Event Architecture
13. Runtime Integration Plan
14. Sandbox Integration Plan
15. Production-Grade Final Design

Then IMPLEMENT the fixes.

IMPORTANT:
- preserve existing functionality
- do not break working tools
- migrate incrementally
- maintain backward compatibility where needed
- prioritize stability over cleverness
- keep files modular and under 250 lines