You are a Senior Runtime Architecture Engineer.

Your task is to deeply scan the entire runtime + preview architecture and FIX the "Multiple Process Registries" problem causing:

- Preview 502 errors
- Runtime desynchronization
- Broken server visibility
- Process state mismatch
- Runtime instability

━━━━━━━━━━━━━━━━━━━━
MAIN BUG
━━━━━━━━━━━━━━━━━━━━

Currently the system has MULTIPLE independent process registries.

Examples:
- runtime.routes.ts
- RuntimeService
- server-lifecycle-tools
- preview runtime manager
- internal process Maps

Each subsystem tracks processes separately.

Result:
- AI starts server in one registry
- Preview proxy checks another registry
- Console checks another registry
- Runtime status checks another registry

This creates:
- 502 Preview errors
- invisible running servers
- orphan processes
- stale runtime states
- restart failures
- desynced ports

━━━━━━━━━━━━━━━━━━━━
YOUR OBJECTIVE
━━━━━━━━━━━━━━━━━━━━

Find ALL process registries.
Find ALL process Maps.
Find ALL runtime state stores.
Find ALL child_process tracking systems.

Then:
UNIFY EVERYTHING into ONE central Process Registry.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. Deep scan runtime architecture
2. Find ALL duplicated process tracking systems
3. Find ALL preview/runtime desync points
4. Find ALL independent port tracking systems
5. Find ALL child_process spawn locations
6. Find ALL runtime lifecycle systems
7. Find ALL runtime status APIs
8. Find ALL runtime memory leaks
9. Find ALL zombie process risks
10. Find ALL preview proxy dependency issues

━━━━━━━━━━━━━━━━━━━━
FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create ONE unified runtime architecture:

NEW STRUCTURE:
server/infrastructure/process/
  ├── process-registry.ts
  ├── process-manager.ts
  ├── process-events.ts
  └── process-types.ts

━━━━━━━━━━━━━━━━━━━━
PROCESS REGISTRY RULES
━━━━━━━━━━━━━━━━━━━━

There must be ONLY ONE source of truth:

projectId -> {
  pid,
  port,
  process,
  status,
  startedAt,
  lastActivity,
  logs,
  runtimeType
}

ALL systems MUST use this:
- preview proxy
- runtime routes
- server_start tool
- restart tool
- stop tool
- logs tool
- status tool
- preview APIs
- console APIs

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIXES
━━━━━━━━━━━━━━━━━━━━

Fix:
- Preview 502 issue
- Runtime desync
- Duplicate process Maps
- Port collisions
- Runtime state mismatch
- Orphan process tracking
- Zombie processes
- Missing cleanup
- Runtime restart instability
- Process lifecycle inconsistency

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

✅ Single runtime authority
✅ Single process registry
✅ Single port manager
✅ Single lifecycle manager
✅ Shared runtime events
✅ Shared process state

❌ No duplicate Maps
❌ No local process stores
❌ No isolated runtime managers
❌ No preview-specific registries
❌ No tool-specific registries

━━━━━━━━━━━━━━━━━━━━
IMPORTANT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

1. Keep code CLEAN and MINIMAL
2. Use HIGH COHESION
3. Use LOW COUPLING
4. Clear folder structure
5. Avoid overengineering
6. Reuse existing architecture where possible
7. Do NOT rewrite unrelated systems
8. Keep files UNDER 250 lines
9. Split responsibilities cleanly
10. Use TypeScript types properly

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIRED
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Runtime architecture map
3. List of all duplicate registries
4. List of all broken flows
5. Process lifecycle diagram
6. New unified architecture
7. Exact files to modify
8. Exact files to delete
9. Exact imports to replace
10. Final clean folder structure

Then IMPLEMENT:
- process-registry.ts
- process-manager.ts
- unified runtime flow
- preview integration
- runtime integration
- tool integration
- cleanup system
- recovery system

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- create giant files
- create circular dependencies
- break existing APIs
- remove working runtime logic
- rewrite unrelated code

ONLY:
- unify runtime state
- centralize process tracking
- stabilize preview/runtime architecture
- remove duplicate registries
- fix preview 502 issue permanently