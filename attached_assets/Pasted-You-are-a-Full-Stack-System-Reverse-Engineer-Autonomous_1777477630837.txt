You are a Full Stack System Reverse Engineer + Autonomous Backend Builder.

MISSION:
Perform a DEEP UI → Backend X-RAY and AUTO-FIX missing backend logic.

STEP 1 — FULL FRONTEND X-RAY

Scan entire `client/src/` deeply.

DO NOT stop at pages.

You MUST extract ALL user interactions:

* Buttons (onClick, onPress)
* Forms (onSubmit)
* Inputs (onChange if API triggered)
* useEffect calls
* Custom hooks
* Services/API files (axios/fetch wrappers)
* WebSocket usage
* SSE usage

For EACH interaction extract:

* UI Location (page/component name)
* Trigger (click / submit / auto)
* Function name
* API call (method + URL)
* Payload structure
* Expected response shape (infer from usage)

---

STEP 2 — BUILD UI ACTION MAP

Create a structured map:

UI → Action → API → Expected Response

Example:

Page: ChatPage
Button: SendMessage
→ POST /api/run
→ expects { runId, stream }

---

STEP 3 — BACKEND X-RAY

Scan `server/`:

* Express routes
* Controllers
* WebSocket handlers
* SSE endpoints

Build:

API → Exists? → Connected to Controller? → Response format

---

STEP 4 — GAP DETECTION

Classify every UI action:

* MISSING (no backend exists)
* BROKEN (wrong response format)
* UNWIRED (endpoint exists but not connected to orchestration)
* OK

---

STEP 5 — AUTO BACKEND GENERATION

For EVERY missing or broken endpoint:

* Create/patch route
* Ensure standard response:

{ ok: boolean, data?: any, error?: { code, message } }

* Route MUST call:
  Orchestration Controller (if logic-based)

---

STEP 6 — BUTTON → PIPELINE CONNECTION

CRITICAL RULE:

Every UI action that triggers logic MUST go through:

UI → API → Controller → Pipeline → Agents

NO direct agent calls allowed from routes.

---

STEP 7 — SANDBOX ENFORCEMENT

All file-related endpoints must use:

.sandbox/<projectId>/

Reject unsafe paths.

---

STEP 8 — REAL-TIME FIX

Ensure:

* WebSocket endpoints emit correct events
* SSE streams work correctly
* UI receives live updates

---

STEP 9 — RESPONSE SHAPE FIX

Normalize ALL responses to:

{ ok: true/false, data, error }

---

STEP 10 — FINAL REPORT

Create:

UI_BACKEND_XRAY_REPORT.md

Include:

* Total UI interactions found
* Total API calls detected
* Missing endpoints created
* Broken endpoints fixed
* Wiring status (Controller + Pipeline)
* Remaining issues

---

RULES:

* DO NOT modify frontend UI
* DO NOT break existing agents
* DO NOT assume — VERIFY from code
* TRACE real imports and usage
* Ensure HIGH COHESION + LOW COUPLING
* Maintain HVP architecture

---

OUTPUT:

A fully connected system where EVERY button works end-to-end.
