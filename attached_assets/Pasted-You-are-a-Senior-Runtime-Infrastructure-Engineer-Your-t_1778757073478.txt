You are a Senior Runtime Infrastructure Engineer.

Your task is to deeply scan and FIX the Preview Runtime Disconnect bug in my autonomous coding platform.

━━━━━━━━━━━━━━━━━━━━
MAIN BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: P2

Problem:
Preview proxy is disconnected from the runtime process manager.

Result:
- AI starts the app/server successfully
- Process is running
- But preview panel shows:
  "502 Bad Gateway"
  OR
  "Preview unavailable"

Root Cause:
Multiple isolated runtime registries exist.

Current architecture has:
- runtime.routes.ts
- RuntimeService
- server-lifecycle-tools
- preview-proxy.ts

Each maintains its own process/port state.

The preview proxy checks one registry,
while the AI agent starts the server using another registry.

Result:
Preview system cannot find the correct running process.

━━━━━━━━━━━━━━━━━━━━
YOUR GOAL
━━━━━━━━━━━━━━━━━━━━

Create a SINGLE SOURCE OF TRUTH for runtime processes.

Preview proxy,
agent tools,
runtime routes,
and server lifecycle tools
must ALL use the SAME runtime registry.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. Deep scan entire runtime architecture
2. Find ALL runtime maps and process registries
3. Find ALL port tracking logic
4. Find ALL process spawning logic
5. Find ALL preview proxy lookup logic
6. Trace:
   AI → server_start → process spawn → preview proxy
7. Find exact breakpoints
8. Detect duplicate runtime systems
9. Detect dead runtime state
10. Detect preview desynchronization

━━━━━━━━━━━━━━━━━━━━
FOCUS FILES
━━━━━━━━━━━━━━━━━━━━

Main focus:
- server/api/runtime.routes.ts
- server/preview/runtime/runtime.service.ts
- server/agents/tools/categories/server-lifecycle-tools.ts
- server/infrastructure/proxy/preview-proxy.ts

Also scan:
- process maps
- projectPorts maps
- child_process usage
- spawn()
- port assignment
- preview lookup
- restart handlers
- stop handlers

━━━━━━━━━━━━━━━━━━━━
FIX STRATEGY
━━━━━━━━━━━━━━━━━━━━

Create:
server/infrastructure/process/process-registry.ts

This must become the ONLY runtime authority.

Structure:

type ProcessEntry = {
  projectId: number;
  pid: number;
  port: number;
  status: "starting" | "running" | "stopped" | "crashed";
  process: ChildProcess;
  startedAt: number;
};

Required APIs:
- startProject()
- stopProject()
- restartProject()
- getProcess()
- getPort()
- getLogs()
- removeProcess()
- isRunning()

━━━━━━━━━━━━━━━━━━━━
IMPORTANT RULES
━━━━━━━━━━━━━━━━━━━━

1. DO NOT create multiple registries
2. DO NOT duplicate process state
3. DO NOT break existing APIs
4. DO NOT rewrite unrelated systems
5. Keep changes minimal and surgical
6. Preserve current frontend API contracts
7. Keep backward compatibility
8. Avoid architecture overengineering
9. Keep each file under 250 lines if possible
10. Use clear folder structure
11. Use TypeScript strict typing
12. Use production-safe cleanup
13. Prevent memory leaks
14. Prevent orphan processes
15. Prevent port collisions

━━━━━━━━━━━━━━━━━━━━
PORT MANAGEMENT RULES
━━━━━━━━━━━━━━━━━━━━

Current bug:
port = 5000 + (projectId % 1000)

This causes collisions.

Implement:
- dynamic free-port allocation
OR
- centralized port reservation

Requirements:
- no collisions
- automatic release on stop
- automatic cleanup on crash

━━━━━━━━━━━━━━━━━━━━
PREVIEW FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

preview-proxy.ts MUST:
- read runtime info ONLY from ProcessRegistry
- never use local maps
- never cache stale ports
- dynamically resolve active runtime

When preview opens:
- proxy should always resolve latest runtime port
- reconnect after restart
- survive runtime restarts

━━━━━━━━━━━━━━━━━━━━
RUNTIME EVENT FLOW
━━━━━━━━━━━━━━━━━━━━

Add unified events:
- process.started
- process.stopped
- process.crashed
- process.restarted
- process.log

Use existing event bus if available.

━━━━━━━━━━━━━━━━━━━━
SAFETY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Handle:
- zombie process cleanup
- SIGTERM
- SIGINT
- crashed child processes
- invalid ports
- duplicate starts
- rapid restarts
- stale process references

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Runtime architecture map
3. All duplicate registries found
4. Preview disconnect chain
5. Exact files causing issue
6. Exact lines causing issue
7. Correct runtime architecture
8. Unified process registry design
9. Refactor implementation plan
10. Minimal safe code patches
11. Final corrected flow

━━━━━━━━━━━━━━━━━━━━
FINAL EXPECTED FLOW
━━━━━━━━━━━━━━━━━━━━

AI calls:
server_start()
→ ProcessRegistry.startProject()
→ process spawned
→ port reserved
→ process tracked centrally
→ preview-proxy asks ProcessRegistry.getPort(projectId)
→ proxy connects correctly
→ preview loads successfully

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

Use:
- evidence-based analysis
- exact files
- exact lines
- exact runtime traces

DO NOT:
- give generic answers
- rewrite entire architecture
- create unnecessary abstractions
- break working systems

ONLY:
- unify runtime state
- fix preview connectivity
- stabilize runtime lifecycle
- make preview reliable