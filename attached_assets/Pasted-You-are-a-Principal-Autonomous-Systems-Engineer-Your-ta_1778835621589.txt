You are a Principal Autonomous Systems Engineer.

Your task is to deeply scan the entire codebase and FIX the missing verification loop problem in the agent architecture.

━━━━━━━━━━━━━━━━━━━━
TARGET PROBLEM
━━━━━━━━━━━━━━━━━━━━

ID: A7
Problem: No verification loop
Impact: Broken code gets shipped without validation

Current issue:
The agent writes files, installs packages, and starts runtimes — but there is NO mandatory verification phase after execution.

The system currently:
- generates code
- runs commands
- starts preview
- marks task complete

WITHOUT:
- checking runtime errors
- validating preview health
- reading console logs
- checking failed builds
- validating TypeScript errors
- retrying broken code
- confirming app actually works

Result:
Broken apps are marked as successful.

━━━━━━━━━━━━━━━━━━━━
YOUR MISSION
━━━━━━━━━━━━━━━━━━━━

Deep scan entire backend architecture and identify:

1. Where agent execution ends
2. Where runtime starts
3. Where task_complete is triggered
4. Where verification SHOULD happen
5. Missing validation flow
6. Missing runtime observation loop
7. Missing error detection
8. Missing retry system
9. Missing preview verification
10. Missing post-execution checks

━━━━━━━━━━━━━━━━━━━━
FOCUS AREAS
━━━━━━━━━━━━━━━━━━━━

Scan deeply:

- server/agents/
- server/chat/
- server/runtime/
- server/preview/
- server/infrastructure/
- server/api/
- tool-loop.agent.ts
- executor.ts
- runtime.routes.ts
- preview-proxy.ts
- server-lifecycle-tools.ts
- shell-tools.ts
- bus.ts

━━━━━━━━━━━━━━━━━━━━
YOU MUST DETECT
━━━━━━━━━━━━━━━━━━━━

Find:
- where execution falsely succeeds
- where task_complete is emitted too early
- where server errors are ignored
- where build failures are ignored
- where preview 502 is ignored
- where npm install failures are ignored
- where runtime crashes are ignored
- where TypeScript compile errors are ignored
- where missing dependency errors are ignored

━━━━━━━━━━━━━━━━━━━━
DESIGN A REAL VERIFICATION LOOP
━━━━━━━━━━━━━━━━━━━━

Build a production-grade verification architecture:

AFTER EVERY EXECUTION:
1. observe runtime logs
2. verify server health
3. verify preview availability
4. detect build/runtime errors
5. detect TypeScript errors
6. detect failed package installs
7. validate HTTP response
8. retry automatically if broken
9. self-heal if possible
10. ONLY THEN mark task_complete

━━━━━━━━━━━━━━━━━━━━
REQUIRED EXECUTION FLOW
━━━━━━━━━━━━━━━━━━━━

Correct flow should become:

LLM
→ tool execution
→ file writes
→ package install
→ server start
→ verification loop
    ├─ check runtime logs
    ├─ check preview URL
    ├─ detect errors
    ├─ retry/fix if needed
    └─ confirm healthy
→ task_complete

━━━━━━━━━━━━━━━━━━━━
VERIFICATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

The verification loop must:

✅ read server logs
✅ inspect runtime errors
✅ verify preview returns HTTP 200
✅ detect crashes
✅ detect missing packages
✅ detect syntax errors
✅ detect TypeScript compile errors
✅ detect failed builds
✅ retry server automatically
✅ retry fixes automatically
✅ stop infinite retry loops
✅ emit verification events to frontend

━━━━━━━━━━━━━━━━━━━━
SELF-HEALING REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

If verification fails:

1. read logs
2. identify root cause
3. patch related files
4. restart runtime
5. re-verify
6. max retry count = 3

If still broken:
- emit verification.failed
- generate detailed error report
- DO NOT mark success

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━

STRICT RULES:

✅ high cohesion
✅ low coupling
✅ clean architecture
✅ single responsibility
✅ centralized verification engine
✅ reusable verification services
✅ isolated runtime observer
✅ isolated health checker
✅ modular retry system

❌ DO NOT:
- put verification logic everywhere
- duplicate retry logic
- tightly couple runtime + preview
- create giant files
- mix orchestration with validation

━━━━━━━━━━━━━━━━━━━━
FILE STRUCTURE RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:

- each file MUST stay under 250 lines
- split responsibilities properly
- use clean folder structure
- use reusable services
- use clear naming
- avoid god classes/files

━━━━━━━━━━━━━━━━━━━━
REQUIRED NEW ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Design:

server/verification/
  runtime/
  preview/
  health/
  retry/
  observer/
  validators/

Possible modules:
- verification-engine.ts
- runtime-observer.ts
- preview-validator.ts
- health-checker.ts
- retry-controller.ts
- verification-events.ts
- runtime-error-parser.ts

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Current broken flow
3. Missing verification points
4. Exact files causing issue
5. Verification architecture blueprint
6. Refactor strategy
7. Folder structure
8. New service design
9. Retry flow
10. Verification event flow
11. Runtime observation flow
12. Error recovery flow
13. Preview validation flow
14. TypeScript validation flow
15. Exact implementation plan
16. Step-by-step fixes
17. Exact files to modify
18. New files to create
19. Dependency map
20. Safety protections

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- give generic advice
- rewrite unrelated systems
- create monolithic architecture
- ignore existing runtime system

ONLY:
- deeply scan
- identify exact failures
- build a real verification loop
- stabilize autonomous execution
- make the system production-grade