You are a Senior Autonomous Agent Reliability Engineer.

Your task is to deeply scan the entire agent execution system and FIX the missing retry/recovery architecture.

━━━━━━━━━━━━━━━━━━━━
MAIN BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: A4 — No Retry System

Current behavior:
- If llm.chatWithTools()
- tool execution
- runtime execution
- server startup
- package installation
fails ONCE,
the entire agent run immediately dies.

Small transient failures kill the whole run.

This makes the autonomous coding system unreliable and non-production-grade.

━━━━━━━━━━━━━━━━━━━━
GOALS
━━━━━━━━━━━━━━━━━━━━

1. Deep scan the current retry/error handling system
2. Find all execution points that fail hard
3. Find all missing retries
4. Add production-grade retry architecture
5. Keep code CLEAN and SIMPLE
6. Maintain current folder structure
7. Keep files SMALL
8. Keep each file UNDER 250 lines
9. Use high cohesion
10. Use low coupling
11. Avoid giant utility files
12. Avoid circular dependencies
13. Avoid architecture pollution

━━━━━━━━━━━━━━━━━━━━
SCAN THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Focus areas:

- server/agents/core/tool-loop/
- server/chat/run/
- server/agents/tools/
- server/infrastructure/
- server/api/
- runtime execution
- tool orchestrator
- llm execution
- process manager
- preview startup
- shell execution
- package install
- server lifecycle
- database calls
- event bus flows

━━━━━━━━━━━━━━━━━━━━
FIND ALL FAILURE POINTS
━━━━━━━━━━━━━━━━━━━━

Detect:

- try/catch without retry
- hard failures
- immediate returns
- fatal execution exits
- missing backoff
- missing timeout recovery
- missing retry state
- retry-unaware tools
- non-resumable agent loop
- broken recovery flows
- silent crashes
- unhandled promise rejections
- retry loops that can recurse forever
- duplicate retry logic
- retry logic mixed into business logic

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ANALYSIS
━━━━━━━━━━━━━━━━━━━━

Find EXACTLY:

1. Which errors should retry
2. Which errors should fail immediately
3. Which tools are retry-safe
4. Which operations are dangerous to retry
5. Which retries need rollback
6. Which retries need checkpoint restore
7. Which retries need exponential backoff
8. Which retries should escalate to user
9. Which retries should trigger self-healing
10. Which retries should restart runtime

━━━━━━━━━━━━━━━━━━━━
DESIGN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create a CLEAN architecture:

GOOD:
- isolated retry layer
- reusable retry engine
- typed retry policies
- retry-aware execution wrapper
- composable retry strategies
- centralized retry configuration

BAD:
- retry code everywhere
- giant utility files
- duplicated try/catch blocks
- recursive retry loops
- hidden retries
- infinite retry behavior

━━━━━━━━━━━━━━━━━━━━
CREATE THIS ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Design:

1. RetryEngine
2. RetryPolicy
3. RetryableError classifier
4. RetryContext
5. BackoffStrategy
6. RetryTelemetry
7. RetryGuard
8. RetryLimit system
9. Retry-safe execution wrapper
10. Failure escalation system

━━━━━━━━━━━━━━━━━━━━
RETRY RULES
━━━━━━━━━━━━━━━━━━━━

Retryable:
- LLM timeout
- network timeout
- package install timeout
- temporary server boot failure
- preview startup race condition
- database connection timeout
- file lock race condition
- transient shell failure

Do NOT retry:
- syntax errors
- invalid tool names
- missing files
- permission denied
- invalid user input
- sandbox violations
- security violations

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT THESE FEATURES
━━━━━━━━━━━━━━━━━━━━

1. Exponential backoff
2. Max retry limits
3. Retry jitter
4. Timeout wrapping
5. Retry event emission
6. Retry metrics
7. Retry cancellation support
8. Retry-safe logging
9. Retry escalation
10. Retry classification

━━━━━━━━━━━━━━━━━━━━
AGENT LOOP IMPROVEMENTS
━━━━━━━━━━━━━━━━━━━━

Upgrade the agent loop so:

OLD:
failure → stop

NEW:
failure
→ classify error
→ retry if safe
→ self-heal if possible
→ restart affected subsystem
→ continue execution
→ escalate only if retries exhausted

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FILE RULES
━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

- each file under 250 lines
- no god classes
- no giant managers
- one responsibility per module
- clean dependency flow
- no circular imports
- no hidden global state

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Current retry weaknesses
3. Retry architecture blueprint
4. Failure classification table
5. Retry-safe vs unsafe operations
6. Refactor strategy
7. New folder structure
8. Exact files to create
9. Exact files to modify
10. Production-grade implementation plan
11. Retry execution flow diagram
12. Backoff strategy design
13. Recovery flow design
14. Safety protections
15. Final architecture

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite unrelated systems
- break current architecture
- add overengineering
- add framework complexity
- create giant abstractions

ONLY:
- stabilize execution
- improve reliability
- add safe retries
- preserve modular architecture
- make the agent resilient