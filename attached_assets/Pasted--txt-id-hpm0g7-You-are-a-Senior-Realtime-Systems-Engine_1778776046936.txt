````txt id="hpm0g7"
You are a Senior Realtime Systems Engineer and SSE Architecture Auditor.

Your task is to deeply inspect and FIX the SSE duplicate-event bug causing malformed streams, duplicate frontend updates, broken event parsing, unnecessary memory usage, and unstable realtime behavior.

━━━━━━━━━━━━━━━━━━━━
MAIN TARGET
━━━━━━━━━━━━━━━━━━━━

BUG ID: P6
Problem: SSE duplicate events
Impact:
- broken streams
- duplicate UI updates
- malformed EventSource parsing
- memory waste
- listener instability
- frontend state corruption
- unnecessary rerenders

━━━━━━━━━━━━━━━━━━━━
PRIMARY FILES TO AUDIT
━━━━━━━━━━━━━━━━━━━━

Backend:
- server/chat/streams/sse.ts
- server/infrastructure/events/bus.ts
- server/chat/index.ts
- server/chat/run/*
- server/agents/core/*
- all EventSource emitters

Frontend:
- client/src/hooks/*
- client/src/lib/*
- client/src/components/*
- useAgentRunner
- useLiveAgentStream
- useAgiStream
- any EventSource wrapper

━━━━━━━━━━━━━━━━━━━━
KNOWN CRITICAL BUG
━━━━━━━━━━━━━━━━━━━━

Current bug pattern:

Example suspected code:

```ts
sseSend(res, "agent", e);
res.write(`data: ${JSON.stringify(e)}\n\n`);
````

This causes:

* same event written twice
* malformed SSE frames
* duplicate frontend messages
* invalid event parsing
* double rerenders

━━━━━━━━━━━━━━━━━━━━
YOUR TASKS
━━━━━━━━━━━━━━━━━━━━

1. Deep scan ALL SSE architecture
2. Detect every duplicate write
3. Detect malformed SSE formatting
4. Detect overlapping SSE endpoints
5. Detect duplicate subscriptions
6. Detect duplicate EventSource listeners
7. Detect frontend duplicate handlers
8. Detect event rebroadcast loops
9. Detect memory leak risks
10. Detect unstable cleanup logic

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ANALYSIS
━━━━━━━━━━━━━━━━━━━━

Find:

* which SSE endpoints are real
* which are duplicated
* which overlap unnecessarily
* which endpoints subscribe to same bus events
* which frontend hooks consume same stream twice
* which listeners are never cleaned
* where duplicate rerenders happen
* where event fanout becomes unstable

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIXES
━━━━━━━━━━━━━━━━━━━━

Fix all duplicate event writes.

Enforce:

* ONE event write per event
* ONE formatting strategy
* ONE listener cleanup flow
* ONE stable SSE utility

Create production-grade SSE architecture.

━━━━━━━━━━━━━━━━━━━━
STRICT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. Keep file size UNDER 250 lines
2. Split large logic into utilities/services
3. Maintain clean folder structure
4. No giant god-files
5. No duplicated emitters
6. No duplicated listeners
7. No nested SSE abstractions
8. No event rebroadcast chaos
9. No unsafe listener handling
10. No breaking existing APIs

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create:

* reusable SSE helper
* unified event formatter
* centralized listener cleanup
* typed SSE payload system
* stable subscription manager

━━━━━━━━━━━━━━━━━━━━
EXPECTED FINAL STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Example structure:

server/chat/streams/
├── sse.ts
├── sse-manager.ts
├── sse-utils.ts
├── sse-types.ts
└── subscriptions/

client/src/realtime/
├── event-source-manager.ts
├── sse-client.ts
├── stream-hooks/
└── stream-types.ts

━━━━━━━━━━━━━━━━━━━━
VERY IMPORTANT
━━━━━━━━━━━━━━━━━━━━

Do NOT:

* rewrite unrelated systems
* modify business logic
* change agent architecture
* break frontend compatibility

ONLY:

* stabilize SSE
* remove duplicates
* fix malformed streams
* fix listener leaks
* improve realtime reliability

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Exact duplicate event locations
3. Exact malformed stream locations
4. Exact frontend duplicate subscription locations
5. Event flow diagram
6. Fixed SSE architecture
7. Refactored folder structure
8. Step-by-step fixes
9. Production-grade implementation
10. Final cleaned code

IMPORTANT:

* mention exact files
* mention exact line numbers
* mention exact duplicated writes
* mention exact listener leaks
* keep all files under 250 lines
* use modular architecture
* use high cohesion
* use low coupling

```
```
