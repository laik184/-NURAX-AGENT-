You are a Senior Autonomous Agent Systems Engineer.

Your task is to deeply scan the codebase and FIX this critical architectural issue:

━━━━━━━━━━━━━━━━━━━━
BUG ID: A3
NO RE-PLANNING LOOP
━━━━━━━━━━━━━━━━━━━━

CURRENT PROBLEM:
The agent stops permanently when max_steps is reached.

Current behavior:
- runAgentLoop() executes until steps >= maxSteps
- then returns:
  stopReason: "max_steps"
- task execution dies permanently
- no continuation
- no context compression
- no retry
- no re-planning
- no autonomous continuation

This breaks complex autonomous coding tasks.

━━━━━━━━━━━━━━━━━━━━
GOAL
━━━━━━━━━━━━━━━━━━━━

Implement a REAL autonomous continuation + re-planning system.

When max_steps is reached:
1. summarize completed work
2. summarize remaining work
3. compress context safely
4. generate continuation prompt
5. restart loop automatically
6. continue execution
7. preserve memory/state
8. prevent infinite continuation recursion

━━━━━━━━━━━━━━━━━━━━
SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━

Deep scan:
- server/agents/core/tool-loop/
- server/chat/run/
- server/agents/core/
- llm wrapper
- tool execution flow
- event lifecycle
- cancellation system
- memory handling
- message history handling

Find:
- exact loop exit point
- exact stopReason logic
- exact message accumulation logic
- exact max_steps enforcement
- exact cancellation handling
- exact event emissions
- exact run lifecycle updates

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIX
━━━━━━━━━━━━━━━━━━━━

Implement:

AUTONOMOUS CONTINUATION LOOP

Flow:

if (steps >= maxSteps):
    summarize execution
    compress messages
    create continuation state
    increment continuation count
    restart runAgentLoop()
    continue execution

━━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━━

1. CONTINUATION SYSTEM
Add:
- continuationCount
- maxContinuations
- continuation state
- compressed context

2. CONTEXT COMPRESSION
Before continuation:
- summarize old tool outputs
- summarize completed tasks
- summarize generated files
- summarize runtime state
- preserve critical memory only

3. SAFE MEMORY WINDOWING
Prevent:
- token explosion
- unbounded messages[]
- memory leaks

4. RE-PLANNING PROMPT
Generate:
- completed tasks
- failed tasks
- remaining tasks
- next priorities

5. LOOP SAFETY
Prevent:
- infinite continuation recursion
- runaway agent loops
- continuation storms

6. EVENT STREAMING
Emit:
- agent.replanning
- agent.continuation
- agent.context_compressed

7. FAILURE RECOVERY
If continuation fails:
- retry safely
- preserve last good state

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CONSTRAINTS
━━━━━━━━━━━━━━━━━━━━

HIGH COHESION
LOW COUPLING

DO NOT:
- create spaghetti logic
- tightly couple continuation to tools
- duplicate loop logic
- rewrite entire architecture
- break existing agent flow

ONLY:
- extend existing architecture cleanly
- isolate continuation logic
- keep modular design
- preserve current APIs

━━━━━━━━━━━━━━━━━━━━
CODE QUALITY RULES
━━━━━━━━━━━━━━━━━━━━

STRICT:
- small modular functions
- single responsibility
- minimal side effects
- typed state objects
- reusable utilities
- readable flow

━━━━━━━━━━━━━━━━━━━━
FOLDER STRUCTURE RULES
━━━━━━━━━━━━━━━━━━━━

Create clean structure if needed:

server/agents/core/tool-loop/
  continuation/
    continuation-manager.ts
    context-compressor.ts
    continuation-state.ts
    replanning-engine.ts

Avoid giant files.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FILE RULE
━━━━━━━━━━━━━━━━━━━━

NO FILE should exceed:
250 lines

If existing file becomes too large:
- split into modules
- extract helpers
- isolate logic

━━━━━━━━━━━━━━━━━━━━
DELIVERABLES
━━━━━━━━━━━━━━━━━━━━

1. Root cause analysis
2. Exact files causing issue
3. Exact flow breakdown
4. Continuation architecture
5. New modular structure
6. Refactored flow
7. Safety protections
8. Context compression strategy
9. Final patched code
10. List of modified files

━━━━━━━━━━━━━━━━━━━━
EXPECTED FINAL FLOW
━━━━━━━━━━━━━━━━━━━━

User Goal
→ Agent Loop
→ max_steps reached
→ summarize progress
→ compress context
→ re-plan remaining work
→ continue autonomously
→ complete task successfully

Build this like a production-grade Replit/Cursor autonomous coding engine.