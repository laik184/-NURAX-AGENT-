You are a Principal Autonomous Systems Engineer.

Your task is to deeply scan the ENTIRE agent workflow architecture and FIX the following critical issue:

━━━━━━━━━━━━━━━━━━━━
CRITICAL ISSUE
━━━━━━━━━━━━━━━━━━━━

A2 — NO TASK DECOMPOSITION

Current problem:
The agent receives large complex goals like:

"Build me a SaaS app with auth, dashboard, payments, database, API, and admin panel"

But the system sends the ENTIRE goal directly into a single tool-loop execution.

Result:
- chaos execution
- random tool usage
- context overload
- max_steps failures
- inconsistent architecture
- broken previews
- unfinished projects
- hallucinated file structure
- unstable runtime behavior

The agent currently has:
❌ no planning hierarchy
❌ no subtask breakdown
❌ no execution phases
❌ no dependency graph
❌ no progress tracking
❌ no ordered workflow

━━━━━━━━━━━━━━━━━━━━
YOUR GOAL
━━━━━━━━━━━━━━━━━━━━

Design and implement a REAL TASK DECOMPOSITION SYSTEM inspired by:
- Replit Agent
- Cursor
- Devin
- Lovable
- Claude Code
- OpenHands

The new system MUST:
✅ decompose goals into structured subtasks
✅ create execution order
✅ track dependencies
✅ support retries
✅ support checkpoints
✅ support progress tracking
✅ support partial completion
✅ support re-planning
✅ support autonomous execution
✅ support runtime verification

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. DEEP SCAN FIRST
Before coding:
- deeply inspect current workflow
- inspect tool-loop
- inspect orchestrator
- inspect pipeline
- inspect runtime execution
- inspect tool orchestration
- inspect event system
- inspect frontend expectations

Find:
- where decomposition SHOULD happen
- where execution SHOULD branch
- where progress SHOULD persist
- where subtasks SHOULD reconnect

━━━━━━━━━━━━━━━━━━━━
DESIGN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create:

1. Planner Engine
2. Task Graph System
3. Subtask Queue
4. Dependency Resolver
5. Progress Tracker
6. Replanning Engine
7. Checkpoint System
8. Execution Coordinator

━━━━━━━━━━━━━━━━━━━━
EXPECTED EXECUTION FLOW
━━━━━━━━━━━━━━━━━━━━

USER GOAL:
"Build me a Todo SaaS"

SHOULD BECOME:

PHASE 1 — ANALYSIS
- detect stack
- inspect existing files
- inspect package manager
- inspect runtime

PHASE 2 — PLANNING
Generate structured subtasks:
1. setup vite
2. install packages
3. setup database
4. create schema
5. build backend API
6. build frontend
7. connect API
8. run tests
9. start preview
10. verify runtime

PHASE 3 — EXECUTION
Each subtask:
- executes independently
- tracks status
- emits progress
- can retry
- can fail safely
- can resume later

PHASE 4 — VERIFICATION
- inspect runtime logs
- inspect preview
- inspect errors
- retry failed subtasks

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

⚠️ VERY IMPORTANT:

1. KEEP FILES CLEAN
- max 250 lines per file
- split logic properly
- avoid god files

2. CLEAR FOLDER STRUCTURE
Use:

server/
  agents/
    planning/
      planner/
      decomposition/
      dependency/
      progress/
      checkpoints/
      execution/
      verification/

3. HIGH COHESION
Each module should do ONE thing only.

4. LOW COUPLING
- no circular imports
- no runtime recursion
- no giant shared state
- communicate via interfaces/events

5. NO MONOLITHIC FUNCTIONS
Break everything into:
- services
- coordinators
- resolvers
- executors
- adapters

━━━━━━━━━━━━━━━━━━━━
REQUIRED OUTPUT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Current architecture flaws
3. New folder structure
4. Task decomposition architecture
5. Execution graph design
6. Progress tracking design
7. Retry/replanning strategy
8. Checkpoint architecture
9. Step-by-step implementation plan
10. Exact files to create
11. Exact files to modify
12. Dependency map
13. Event flow map
14. Runtime flow map
15. Production-grade implementation

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

DO NOT:
❌ create giant files
❌ add fake abstractions
❌ create dead services
❌ introduce circular dependencies
❌ break existing runtime
❌ rewrite unrelated systems

ONLY:
✅ improve architecture
✅ modularize properly
✅ add scalable planning
✅ stabilize execution flow
✅ build real autonomous orchestration

━━━━━━━━━━━━━━━━━━━━
FINAL OBJECTIVE
━━━━━━━━━━━━━━━━━━━━

Transform the current chaotic single-loop agent into a structured autonomous multi-stage execution system with:
- planning
- decomposition
- coordination
- retries
- verification
- progress tracking
- self-healing foundations

The final architecture must feel production-grade and Replit-level.