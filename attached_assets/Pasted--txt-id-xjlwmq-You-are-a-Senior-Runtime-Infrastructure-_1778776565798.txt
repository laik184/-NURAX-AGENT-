````txt id="xjlwmq"
You are a Senior Runtime Infrastructure Engineer and Process Management Auditor.

Your task is to deeply scan the entire runtime + preview architecture and FIX the port collision system that causes random preview crashes.

━━━━━━━━━━━━━━━━━━━━
MAIN BUG
━━━━━━━━━━━━━━━━━━━━

Current system assigns preview/runtime ports like this:

```ts
const port = 5000 + (projectId % 1000)
````

Problem:

* Project 1  → 5001
* Project 1001 → 5001 AGAIN

This creates:

* random preview crashes
* 502 proxy errors
* runtime conflicts
* incorrect server routing
* process collisions
* broken preview sessions

This is a CRITICAL ARCHITECTURE BUG.

━━━━━━━━━━━━━━━━━━━━
YOUR OBJECTIVES
━━━━━━━━━━━━━━━━━━━━

1. Deep scan all runtime systems
2. Find ALL port assignment logic
3. Find ALL process registries
4. Find ALL runtime maps
5. Find ALL preview-proxy integrations
6. Find ALL hardcoded ports
7. Detect duplicate runtime managers
8. Detect orphan process risks
9. Detect stale port reuse
10. Detect preview routing conflicts

━━━━━━━━━━━━━━━━━━━━
IMPORTANT SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━

Focus deeply on:

* server/api/runtime.routes.ts
* server/preview/runtime/
* server/infrastructure/proxy/
* server/agents/tools/categories/server-lifecycle-tools.ts
* project runner services
* preview proxy services
* sandbox runtime managers
* process registries
* child process management
* runtime lifecycle systems

━━━━━━━━━━━━━━━━━━━━
FIND THESE PROBLEMS
━━━━━━━━━━━━━━━━━━━━

Detect:

* modulo-based port generation
* duplicate ports
* stale ports
* zombie process ports
* race conditions
* multi-runtime conflicts
* disconnected process maps
* runtime-preview desync
* invalid process recovery
* unsafe port reuse
* hardcoded runtime assumptions

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIX
━━━━━━━━━━━━━━━━━━━━

Replace ALL broken port logic with a PRODUCTION-GRADE dynamic port allocation system.

Requirements:

✅ Single unified ProcessRegistry
✅ Dynamic free-port allocation
✅ Automatic port reservation
✅ Automatic port release
✅ Crash-safe cleanup
✅ Preview-proxy synchronization
✅ Runtime restart safety
✅ Multi-project safe
✅ No duplicate ports
✅ No hardcoded port math
✅ No modulo logic
✅ No random collisions

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

Create:

* ONE centralized process registry
* ONE centralized port allocator
* ONE runtime source-of-truth

All systems MUST use:

* same registry
* same port manager
* same runtime state

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CONSTRAINTS
━━━━━━━━━━━━━━━━━━━━

⚠️ VERY IMPORTANT:

* Keep code CLEAN
* Keep architecture SIMPLE
* Keep folder structure CLEAR
* Avoid overengineering
* Avoid deep nesting
* Avoid duplicated services
* Avoid giant files

━━━━━━━━━━━━━━━━━━━━
STRICT CODE RULES
━━━━━━━━━━━━━━━━━━━━

* Maximum 250 lines per file
* Split responsibilities cleanly
* Use small focused modules
* Strong TypeScript typing
* No dead code
* No fake placeholders
* No duplicate registries
* No hidden globals

━━━━━━━━━━━━━━━━━━━━
EXPECTED OUTPUT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. All affected files
3. Runtime architecture map
4. Port lifecycle diagram
5. Correct runtime architecture
6. Unified ProcessRegistry design
7. Dynamic port allocator design
8. Step-by-step refactor plan
9. Final clean folder structure
10. Full implementation patches

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

The final system MUST:

* support unlimited projects safely
* survive server restarts
* prevent preview crashes
* prevent 502 errors
* correctly sync preview ↔ runtime
* properly cleanup ports on exit
* correctly recover after crashes
* prevent zombie runtime state
* support future scaling

━━━━━━━━━━━━━━━━━━━━
DO NOT
━━━━━━━━━━━━━━━━━━━━

❌ Do not use:

* modulo ports
* static ports
* duplicated Maps
* multiple runtime registries
* hardcoded runtime assumptions
* giant runtime managers

❌ Do not:

* rewrite unrelated systems
* break existing preview flow
* remove SSE functionality
* change business logic

━━━━━━━━━━━━━━━━━━━━
ONLY FOCUS ON
━━━━━━━━━━━━━━━━━━━━

* runtime stability
* process lifecycle
* port allocation
* preview synchronization
* clean architecture
* production-grade runtime management

Act like you are fixing the runtime architecture of a billion-dollar autonomous coding platform.

```
```
