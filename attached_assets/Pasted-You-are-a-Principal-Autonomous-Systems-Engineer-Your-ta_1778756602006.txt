You are a Principal Autonomous Systems Engineer.

Your task is to DEEP SCAN the entire codebase and FIX the architectural issue:

━━━━━━━━━━━━━━━━━━━━
BUG ID: A1
NO PLANNER AGENT
━━━━━━━━━━━━━━━━━━━━

CURRENT PROBLEM:
The system directly sends large user goals into the tool-loop agent without decomposition, planning, prioritization, or execution staging.

Example:
User:
"Build me a SaaS app with auth, payments, dashboard, database, and admin panel"

Current behavior:
- entire goal sent directly to tool-loop
- chaotic execution
- random file writes
- context overflow
- max_steps reached
- broken architecture
- no dependency ordering
- no execution verification
- no task memory
- no recovery strategy

RESULT:
Big tasks fail.

━━━━━━━━━━━━━━━━━━━━
YOUR MISSION
━━━━━━━━━━━━━━━━━━━━

1. Deep scan current architecture
2. Detect current execution flow
3. Find where user goals directly enter tool-loop
4. Find missing planning layer
5. Design and implement a REAL Planner Agent
6. Integrate planner BEFORE execution phase
7. Refactor architecture safely
8. Keep backward compatibility
9. Keep system modular and scalable
10. Prevent overengineering

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

SYSTEM MUST:
- support large goals
- decompose tasks
- prioritize execution order
- generate execution phases
- support retries
- support checkpoints
- support autonomous coding
- support self-healing later
- support future multi-agent workflows

━━━━━━━━━━━━━━━━━━━━
PLANNER AGENT RESPONSIBILITIES
━━━━━━━━━━━━━━━━━━━━

Planner Agent MUST:

1. Analyze user goal
2. Detect app type
3. Detect tech stack
4. Detect dependencies
5. Break into subtasks
6. Create ordered execution plan
7. Estimate required tools
8. Generate execution phases
9. Generate context memory
10. Save plan artifacts

━━━━━━━━━━━━━━━━━━━━
EXPECTED EXECUTION FLOW
━━━━━━━━━━━━━━━━━━━━

OLD FLOW:
User
→ Tool Loop
→ Chaos

NEW FLOW:
User
→ Planner Agent
→ Execution Plan
→ Subtasks
→ Tool Loop
→ Verification
→ Next Subtask
→ Completion

━━━━━━━━━━━━━━━━━━━━
PLANNER OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Planner must generate:

{
  goal,
  appType,
  stack,
  phases: [
    {
      id,
      title,
      objective,
      dependencies,
      files,
      tools,
      verification,
      priority
    }
  ],
  risks,
  estimatedComplexity,
  executionStrategy
}

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Find:
- current orchestration entrypoints
- current tool-loop flow
- current run lifecycle
- current execution pipeline
- missing planning integration points
- broken execution assumptions
- places causing max_steps failures
- places causing context overload
- large-file token explosions
- recursive execution risks
- weak dependency ordering

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━

STRICT RULES:

✅ Use clear folder structure
✅ Small modular services
✅ Single responsibility
✅ Planner isolated from execution
✅ Strong type safety
✅ Reusable planning models
✅ Easy debugging
✅ Future multi-agent compatible
✅ Maintainable architecture

❌ DO NOT:
- create giant files
- create monolithic classes
- exceed 250 lines per file
- tightly couple planner with tool-loop
- duplicate orchestration logic
- break existing flows
- hardcode stacks
- create recursive orchestration

━━━━━━━━━━━━━━━━━━━━
FILE SIZE RULE
━━━━━━━━━━━━━━━━━━━━

CRITICAL:
NO FILE SHOULD EXCEED 250 LINES.

If logic grows:
- split into modules
- create utilities
- create planners/
- create services/
- create models/
- create strategies/

━━━━━━━━━━━━━━━━━━━━
EXPECTED FOLDER STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Design a clean structure like:

server/
  agents/
    planning/
      planner.agent.ts
      planner.service.ts
      planner.types.ts
      planner.prompts.ts
      planner.validators.ts
      planner.memory.ts
      strategies/
      utils/

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Implement:

1. Planner Agent
2. Plan generation engine
3. Task decomposition system
4. Dependency ordering
5. Execution phase builder
6. Plan persistence
7. Planner-to-toolloop bridge
8. Execution context generator
9. Retry-ready structure
10. Future checkpoint support

━━━━━━━━━━━━━━━━━━━━
INTEGRATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Planner must integrate with:
- current ChatOrchestrator
- current run lifecycle
- current event bus
- current SSE system
- current tool-loop
- current sandbox
- current runtime system

WITHOUT breaking:
- existing routes
- current tool execution
- existing frontend streams
- preview system

━━━━━━━━━━━━━━━━━━━━
IMPORTANT OUTPUTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Current vs new architecture
3. Planner workflow diagram
4. Refactor strategy
5. Exact files to modify
6. Exact new files to create
7. Dependency graph
8. Execution lifecycle
9. Context lifecycle
10. Step-by-step implementation plan

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING GOALS
━━━━━━━━━━━━━━━━━━━━

The final architecture should feel like:
- Replit Agent
- Cursor Agent
- Devin-like task planning
- Production-grade orchestration

BUT:
- lightweight
- maintainable
- scalable
- understandable

━━━━━━━━━━━━━━━━━━━━
FINAL INSTRUCTION
━━━━━━━━━━━━━━━━━━━━

DO NOT GIVE GENERIC ANSWERS.

Actually:
- inspect the codebase deeply
- identify real flaws
- identify exact integration points
- identify exact execution flow
- produce production-grade architecture fixes

Focus on:
- stability
- orchestration clarity
- autonomous scalability
- future-proof architecture
- clean engineering