You are a Senior Autonomous Systems Engineer.

Your task is to deeply scan the codebase and FIX the following critical architectural bug:

━━━━━━━━━━━━━━━━━━━━
BUG ID: P3
━━━━━━━━━━━━━━━━━━━━

Problem:
agentQuestion is FAKE.

The system pretends the AI is asking the user a question and waiting for input, but the backend immediately returns the default answer instead of actually blocking execution.

Current behavior:
- agent emits "question" event
- frontend shows question UI
- backend instantly returns defaultAnswer
- AI continues execution without waiting
- user answer has ZERO effect

This breaks:
- human-in-the-loop workflow
- approval system
- confirmation flow
- safe autonomous execution
- diff approval architecture

━━━━━━━━━━━━━━━━━━━━
YOUR OBJECTIVE
━━━━━━━━━━━━━━━━━━━━

Convert agentQuestion into a REAL blocking interaction system.

The AI agent must:
1. ask user question
2. PAUSE execution
3. WAIT for real user answer
4. continue only after answer arrives
5. support timeout fallback
6. support cancellation
7. support reconnect-safe flow

━━━━━━━━━━━━━━━━━━━━
SCAN THESE AREAS FIRST
━━━━━━━━━━━━━━━━━━━━

Deep scan:
- server/agents/tools/categories/
- server/chat/
- server/chat/run/
- server/chat/streams/
- client/src/
- useAgentRunner
- SSE event flow
- Event bus
- Existing question-bus.ts
- Agent loop execution flow
- Run lifecycle system

Find:
- current fake flow
- defaultAnswer bypass
- missing await logic
- broken promise chain
- missing answer route
- frontend answer disconnect
- race conditions
- memory leak risks
- cancellation risks

━━━━━━━━━━━━━━━━━━━━
EXPECTED ROOT CAUSE
━━━━━━━━━━━━━━━━━━━━

Likely current bad flow:

agentQuestion()
→ emit event
→ return defaultAnswer immediately
→ agent loop continues

Instead it should become:

agentQuestion()
→ create questionId
→ emit question event
→ await waitForAnswer(questionId)
→ receive answer
→ return real answer
→ continue execution

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIX ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

You MUST implement:

1. REAL QUESTION WAIT SYSTEM
- Promise-based blocking
- questionId tracking
- timeout support
- cancellation support

2. QUESTION REGISTRY
Create centralized in-memory question registry:
- pending questions
- answer resolvers
- timeout cleanup
- cancellation cleanup

3. BACKEND ANSWER API
Create route:
POST /api/run/answer

Body:
{
  questionId,
  answer
}

4. FRONTEND INTEGRATION
Frontend must:
- render pending question
- submit answer
- show waiting state
- survive SSE reconnects

5. AGENT LOOP PAUSE
The agent loop must ACTUALLY stop execution while waiting.

6. SAFETY
Prevent:
- memory leaks
- orphan promises
- unresolved waits
- duplicate answers
- stale question state

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

HIGH CAUTION MODE:
- do NOT break existing SSE system
- do NOT rewrite unrelated architecture
- do NOT modify tool loop behavior unnecessarily
- do NOT create recursive waits
- do NOT introduce deadlocks

LOW COUPLING:
- isolated question service
- reusable wait system
- clean interfaces
- minimal dependency spread

CLEAN FOLDER STRUCTURE:
Keep architecture clean and modular.

Recommended structure:

server/chat/questions/
  question-registry.ts
  question.service.ts
  question.routes.ts
  question.types.ts

Do NOT scatter logic randomly.

━━━━━━━━━━━━━━━━━━━━
CODE QUALITY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

- production-grade TypeScript
- strong typing
- cleanup handlers
- timeout protection
- Promise.race handling
- no any types
- no memory leaks
- no duplicate listeners
- reconnect-safe architecture

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Existing broken flow
3. Correct architecture
4. Step-by-step fix plan
5. File-by-file modifications
6. Exact code patches
7. New files to create
8. Cleanup logic
9. Timeout handling
10. Cancellation handling
11. SSE integration flow
12. Frontend answer flow
13. Final execution diagram

━━━━━━━━━━━━━━━━━━━━
VERY IMPORTANT
━━━━━━━━━━━━━━━━━━━━

Keep implementation SMALL and CLEAN.

Rules:
- under 250 lines per file
- single responsibility per file
- avoid giant god classes
- avoid tight coupling
- avoid architecture pollution

Goal:
Production-grade REAL human-in-the-loop blocking system like Replit/Cursor approval flows.