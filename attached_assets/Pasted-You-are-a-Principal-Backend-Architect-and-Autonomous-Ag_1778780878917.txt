You are a Principal Backend Architect and Autonomous Agent Systems Engineer.

Your task is to deeply scan, analyze, and FIX the architectural conflict between:

- Pipeline Runtime
- ToolLoop Runtime

The system currently has TWO competing execution architectures:

1. PIPELINE MODE
   server/agents/core/pipeline/
   → 9-phase execution engine
   → routing, planning, validation, generation, execution

2. TOOL LOOP MODE
   server/agents/core/tool-loop/
   → ReAct-style autonomous tool-calling loop
   → LLM → tools → feedback → repeat

Both systems partially overlap, partially duplicate responsibilities, and create architectural confusion, runtime fragmentation, execution inconsistency, and maintenance complexity.

━━━━━━━━━━━━━━━━━━━━
MAIN PROBLEM
━━━━━━━━━━━━━━━━━━━━

Current flow is broken because:

- some routes use pipeline mode
- some routes use tool-loop mode
- event formats differ
- runtime behavior differs
- file generation behavior differs
- process lifecycle differs
- recovery logic differs
- orchestration responsibilities overlap

This creates:
- inconsistent agent behavior
- broken preview integration
- duplicated execution systems
- unclear source of truth
- difficult debugging
- unstable runtime state
- impossible scaling
- orchestration confusion

━━━━━━━━━━━━━━━━━━━━
SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Deep scan entire codebase and identify:

1. All files using executePipeline()
2. All files using runAgentLoop()
3. All route entry points
4. All runtime entry points
5. All orchestration layers
6. All duplicated logic
7. All overlapping responsibilities
8. All incompatible event structures
9. All conflicting execution flows
10. All dead execution paths

━━━━━━━━━━━━━━━━━━━━
ANALYZE THESE AREAS
━━━━━━━━━━━━━━━━━━━━

Pipeline:
- server/agents/core/pipeline/
- pipeline orchestrator
- dispatch system
- registry
- generation phases
- recovery phases
- feedback phases

ToolLoop:
- server/agents/core/tool-loop/
- tool-loop.agent.ts
- tool orchestrator
- LLM tool execution
- autonomous loop
- tool feedback cycle

Chat Runtime:
- server/chat/
- run executor
- orchestration controller
- event streams
- SSE

Runtime:
- preview runtime
- process registry
- server lifecycle tools
- runtime.routes.ts

━━━━━━━━━━━━━━━━━━━━
DETECT
━━━━━━━━━━━━━━━━━━━━

Find:
- duplicated architecture
- duplicated orchestration
- duplicated execution systems
- duplicated event emission
- duplicated runtime management
- duplicated recovery systems
- conflicting execution flows
- dead pipeline code
- unreachable execution paths
- fragmented runtime ownership
- architecture violations
- mixed responsibilities
- broken boundaries

━━━━━━━━━━━━━━━━━━━━
IMPORTANT GOAL
━━━━━━━━━━━━━━━━━━━━

You must determine:

WHICH SYSTEM SHOULD BE PRIMARY?

Option A:
Pipeline is primary
ToolLoop becomes execution engine

OR

Option B:
ToolLoop is primary
Pipeline becomes planner layer

Choose the BEST architecture based on:
- scalability
- maintainability
- autonomous coding capability
- Replit-like behavior
- Cursor-like behavior
- real-time execution
- self-healing support
- tool-calling support
- runtime stability

━━━━━━━━━━━━━━━━━━━━
EXPECTED FINAL ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Target architecture should have:

ONE:
- execution engine
- runtime manager
- orchestration layer
- event system
- process registry
- preview lifecycle
- recovery system

NO:
- duplicated runtimes
- duplicated orchestrators
- competing execution flows
- mixed ownership

━━━━━━━━━━━━━━━━━━━━
FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Refactor system into CLEAN ARCHITECTURE:

PLANNER
↓
TASK MANAGER
↓
TOOL LOOP EXECUTOR
↓
PROCESS REGISTRY
↓
EVENT BUS
↓
PREVIEW RUNTIME

Pipeline should ONLY:
- planning
- validation
- decomposition
- strategy

ToolLoop should ONLY:
- execution
- tool calling
- runtime observation
- retries
- self-healing

━━━━━━━━━━━━━━━━━━━━
STRICT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. Keep files UNDER 250 lines
2. Reduce coupling
3. High cohesion only
4. Single responsibility per module
5. No circular imports
6. No duplicate runtime state
7. No duplicated event systems
8. No business logic inside routes
9. No giant god files
10. Clear folder boundaries

━━━━━━━━━━━━━━━━━━━━
REFACTOR TARGET STRUCTURE
━━━━━━━━━━━━━━━━━━━━

server/
├── orchestration/
│   ├── planner/
│   ├── task-manager/
│   ├── execution/
│   ├── recovery/
│   └── runtime/
│
├── runtime/
│   ├── process-registry/
│   ├── preview/
│   ├── lifecycle/
│   └── sandbox/
│
├── agents/
│   ├── planner/
│   ├── executor/
│   ├── recovery/
│   └── tools/
│
├── events/
│   ├── bus/
│   ├── streams/
│   └── emitters/
│
└── api/
    ├── run/
    ├── preview/
    ├── files/
    └── runtime/

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root Cause Report
2. Pipeline vs ToolLoop Comparison
3. Current Broken Flow
4. Correct Unified Flow
5. Runtime Ownership Map
6. Event Ownership Map
7. Process Ownership Map
8. Dead Code Report
9. Duplicate Logic Report
10. Refactor Strategy
11. File-by-File Refactor Plan
12. Final Clean Architecture
13. Migration Plan
14. Safety Checks
15. Final Folder Structure

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite working systems unnecessarily
- remove real tool execution
- break SSE streaming
- break preview runtime
- create giant files

ONLY:
- unify architecture
- remove duplication
- simplify execution ownership
- enforce clean boundaries
- stabilize runtime behavior
- make autonomous coding reliable

Act like a senior engineer rebuilding the execution core of a billion-dollar autonomous coding platform.