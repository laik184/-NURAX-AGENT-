ROLE
You are a Principal Backend Architect + Security Auditor + HVP Enforcement Engine.

OBJECTIVE
Perform a FULL X-RAY ANALYSIS of the directory "server/agents/" to detect whether agents are violating architecture rules by performing direct execution instead of delegating to a service layer.
NO assumptions. ONLY real code analysis.

---

PHASE 1 — FULL SCAN
Recursively scan all files inside:
server/agents/

Analyze:

- all ".agent.ts" files
- all "orchestrator.ts" files
- all "utils/" files

---

PHASE 2 — EXECUTION VIOLATION DETECTION

For each ".agent.ts" file, detect if it directly performs execution.

Mark as ❌ VIOLATION if the agent directly uses:

File System:

- fs.writeFile
- fs.unlink
- fs.rename
- fs.rm
- fs.readFile

Shell Execution:

- exec(
- spawn(
- child_process
- bash / sh

Network Calls:

- fetch(
- axios(
- external HTTP calls

Database Access:

- direct DB queries
- ORM usage

System / Process:

- process.exit
- process.env mutation
- git commands

Code/File Writing:

- creating or modifying ".ts", ".js", ".md" files directly

---

PHASE 3 — SAFE PATTERN DETECTION

Mark as ✅ SAFE if agent ONLY:

- calculates
- validates
- transforms data
- scores or analyzes

AND does NOT perform any execution

---

PHASE 4 — CLASSIFICATION

For EACH ".agent.ts", classify:

- SAFE_AGENT
- EXECUTION_AGENT ❌
- MIXED_AGENT ⚠️

---

PHASE 5 — BLUEPRINT GENERATION

Generate full architecture map:

Domain → Module → Files

For each module include:

- orchestrator
- agents
- utils
- state
- types

---

PHASE 6 — CALL FLOW TRACE

Trace real flow:

Pipeline → Orchestrator → Agent → (next action)

Check:

- Does agent call system APIs?
- Does it modify files?
- Does it trigger execution?

---

PHASE 7 — VIOLATION REPORT

Generate table:

Agent | Path | Violation Type | What it does | Severity

Severity rules:

- CRITICAL → deletes files, writes files, shell execution
- HIGH → DB / network direct calls
- MEDIUM → mixed logic + execution
- LOW → minor side effects

---

PHASE 8 — RISK ANALYSIS

Evaluate:

- Can system delete files accidentally?
- Can it execute unsafe commands?
- Is sandboxing present?
- Are guards present?

---

PHASE 9 — SCORING

Provide:

- Execution Safety Score (0–100)
- HVP Compliance Score
- Separation of Concerns Score

---

FINAL OUTPUT

Create ONE file:

AGENT_EXECUTION_AUDIT_REPORT.md

Include sections:

1. System Overview
2. Execution Violations Table
3. Safe vs Unsafe Agent Count
4. Top 20 Dangerous Agents
5. Full Architecture Blueprint
6. Call Flow Graph
7. Risk Analysis
8. Final Scores
9. Final Verdict (SAFE / UNSAFE)

---

STRICT RULES

- DO NOT assume anything
- ONLY analyze real code
- If no violations found → explicitly state "NO EXECUTION VIOLATIONS FOUND"

---

END