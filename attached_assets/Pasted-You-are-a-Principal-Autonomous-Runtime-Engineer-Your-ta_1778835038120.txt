You are a Principal Autonomous Runtime Engineer.

Your task is to deeply scan the ENTIRE runtime + agent execution architecture and FIX the following critical issue:

━━━━━━━━━━━━━━━━━━━━
CRITICAL BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: A6
Problem:
NO RUNTIME OBSERVATION LOOP

Current behavior:
- AI writes files
- AI installs packages
- AI starts server
- AI stops execution
- AI NEVER observes runtime after execution

Result:
- AI becomes blind after server start
- AI cannot detect crashes
- AI cannot detect compile errors
- AI cannot detect runtime failures
- AI cannot self-heal
- AI cannot verify preview status
- AI cannot retry failed builds
- AI cannot inspect logs automatically

This completely breaks autonomous coding behavior.

━━━━━━━━━━━━━━━━━━━━
WHAT A REAL REPLIT/CURSOR SYSTEM DOES
━━━━━━━━━━━━━━━━━━━━

AFTER execution:
1. Observe runtime logs
2. Observe preview status
3. Detect crashes/errors
4. Parse stack traces
5. Identify broken files
6. Patch code automatically
7. Restart runtime
8. Re-observe runtime
9. Repeat until stable or retry limit reached

This is called:
OBSERVE → THINK → FIX → VERIFY LOOP

My system DOES NOT HAVE THIS.

━━━━━━━━━━━━━━━━━━━━
SCAN THESE SYSTEMS DEEPLY
━━━━━━━━━━━━━━━━━━━━

Frontend:
- chat
- console
- preview
- file explorer
- SSE streams
- runtime hooks

Backend:
- runtime manager
- process registry
- preview proxy
- tool loop
- tool orchestrator
- shell tools
- server lifecycle tools
- event bus
- runtime routes
- SSE system
- pipeline system

━━━━━━━━━━━━━━━━━━━━
MAIN TASK
━━━━━━━━━━━━━━━━━━━━

Find:
1. Where runtime execution ENDS
2. Where observation SHOULD start
3. Why AI cannot inspect logs
4. Why AI cannot detect crashes
5. Why AI cannot verify preview
6. Why AI cannot retry
7. Why AI cannot restart automatically
8. Why runtime state is disconnected
9. Why preview state is not observable
10. Why runtime events are not fed back into the LLM loop

━━━━━━━━━━━━━━━━━━━━
IMPORTANT DETECTIONS
━━━━━━━━━━━━━━━━━━━━

Detect:
- missing runtime observers
- missing crash detectors
- missing log parsers
- missing preview health checks
- missing runtime event subscriptions
- disconnected console events
- missing self-healing triggers
- missing verification loop
- missing process health monitoring
- missing restart automation
- broken event propagation
- broken runtime feedback chain

━━━━━━━━━━━━━━━━━━━━
DESIGN THE FIX
━━━━━━━━━━━━━━━━━━━━

Create a REAL runtime observation architecture.

The final architecture MUST include:

1. Runtime Observer Service
2. Process Health Monitor
3. Console Log Analyzer
4. Preview Health Checker
5. Runtime Crash Detector
6. Self-Healing Trigger System
7. Retry Controller
8. Runtime Event Pipeline
9. Verification Loop
10. AI Feedback Injection System

━━━━━━━━━━━━━━━━━━━━
IMPORTANT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

STRICT REQUIREMENTS:

- Use HIGH COHESION
- Use LOW COUPLING
- Clear folder structure
- Clear ownership boundaries
- Single responsibility per module
- No circular dependencies
- No giant files
- No duplicate runtime state
- No hidden event chains
- No mixed orchestration logic

━━━━━━━━━━━━━━━━━━━━
CODE STRUCTURE RULES
━━━━━━━━━━━━━━━━━━━━

MAX FILE SIZE:
- HARD LIMIT = 250 lines per file

If a file exceeds 250 lines:
- split into modules

MANDATORY FOLDER STRUCTURE:

server/
  runtime/
    observer/
    health/
    recovery/
    verification/
    monitoring/
    events/
    analyzers/
    controllers/

━━━━━━━━━━━━━━━━━━━━
EXPECTED OUTPUT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Exact broken execution chain
3. Runtime observation architecture
4. Missing components list
5. Runtime feedback flow diagram
6. Self-healing workflow
7. Retry workflow
8. Verification workflow
9. Suggested folder structure
10. File-by-file implementation plan
11. Exact files to create
12. Exact files to modify
13. Event flow architecture
14. Runtime lifecycle architecture
15. Process monitoring architecture
16. SSE/WebSocket runtime integration
17. Preview verification system
18. Crash recovery system
19. Runtime memory strategy
20. Production-grade refactor plan

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- give generic answers
- rewrite unrelated systems
- merge responsibilities
- create monolithic services
- create tightly coupled architecture

ONLY:
- design scalable runtime observation
- create modular architecture
- fix autonomous blindness
- create real self-healing behavior
- create production-grade runtime feedback loop

Act like you are designing the runtime intelligence layer for Replit, Cursor, and Devin combined.