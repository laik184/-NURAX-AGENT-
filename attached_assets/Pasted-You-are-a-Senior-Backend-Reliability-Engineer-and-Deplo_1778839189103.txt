You are a Senior Backend Reliability Engineer and Deployment Systems Auditor.

Your task is to deeply scan and fix the FAKE DEPLOY SYSTEM bug.

━━━━━━━━━━━━━━━━━━━━
TARGET BUG
━━━━━━━━━━━━━━━━━━━━

BUG ID: T1
Problem: Fake deploy system returns false success.

Current behavior:
- deploy_publish tool pretends deployment succeeded
- returns REPLIT_DEV_DOMAIN URL as production deploy URL
- no actual deployment occurs
- AI confidently says “Deployment successful”
- users receive fake URLs
- this creates false-positive execution state

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Perform a DEEP SYSTEM SCAN to:

1. Find ALL fake deployment logic
2. Find ALL fake success responses
3. Find ALL placeholder deployment systems
4. Find ALL mocked deployment flows
5. Find ALL routes pretending to publish/deploy
6. Find ALL frontend pages connected to fake deploy APIs
7. Find ALL places where deployment status is fabricated
8. Find ALL fake URLs and fake domains
9. Detect architecture violations
10. Fix the deployment system safely

━━━━━━━━━━━━━━━━━━━━
SCAN THESE AREAS
━━━━━━━━━━━━━━━━━━━━

Backend:
- server/agents/tools/categories/deploy-tools.ts
- server/routes/
- server/api/
- server/runtime/
- server/preview/
- server/chat/
- deployment-related orchestrators
- publishing services
- runtime services

Frontend:
- client/src/pages/publishing/
- client/src/components/
- preview pages
- deployment modals
- publishing panels
- deployment hooks
- deployment state stores

━━━━━━━━━━━━━━━━━━━━
FIND THESE ISSUES
━━━━━━━━━━━━━━━━━━━━

Detect:
- fake success messages
- fake deployment states
- mocked deploy URLs
- placeholder publish logic
- frontend assuming deploy success
- routes returning hardcoded success
- missing runtime validation
- missing deploy verification
- missing deployment provider integration
- fake preview-to-production mapping
- misleading UI states
- dead deployment code
- unreachable deployment routes

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIX RULES
━━━━━━━━━━━━━━━━━━━━

STRICT RULES:
- DO NOT fake deployment success
- DO NOT fabricate URLs
- DO NOT return “success” without verification
- DO NOT rewrite unrelated systems
- DO NOT break preview runtime
- DO NOT exceed 250 LOC per file
- KEEP folder structure clean
- KEEP architecture modular
- KEEP deployment logic isolated

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIX STRATEGY
━━━━━━━━━━━━━━━━━━━━

If REAL deployment is not implemented:
- return honest status
- return deployment unsupported state
- explain deployment unavailable
- expose clear next steps
- never fabricate production URLs

Add:
- deployment validation
- runtime verification
- deployment status enum
- explicit unsupported provider handling
- safe error responses
- deploy capability checks

━━━━━━━━━━━━━━━━━━━━
EXPECTED OUTPUT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Exact files containing fake deployment logic
3. Exact line numbers
4. Frontend components affected
5. API routes affected
6. Runtime impact analysis
7. Security implications
8. Correct deployment architecture
9. Refactor plan
10. Minimal safe fixes
11. Production-grade deployment flow
12. List of modified files
13. Final patch strategy

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Use:
- small focused modules
- low coupling
- high cohesion
- clear naming
- single responsibility
- safe runtime checks
- explicit state handling

Maximum:
- 250 LOC per file
- avoid giant service classes
- avoid duplicate deployment state
- avoid fake abstractions

━━━━━━━━━━━━━━━━━━━━
FINAL REQUIREMENT
━━━━━━━━━━━━━━━━━━━━

This is NOT a cosmetic fix.

You must:
- remove fake behavior completely
- make deployment state truthful
- ensure frontend reflects real backend state
- stabilize deployment architecture
- prevent future false-success bugs

Act like you are fixing a production-grade autonomous coding platform used by real users.