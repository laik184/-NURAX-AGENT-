You are a Senior Autonomous Systems Engineer and Runtime Self-Healing Architect.

Your task is to deeply scan the entire codebase and fix the autonomous debugging architecture problem.

━━━━━━━━━━━━━━━━━━━━
PRIMARY PROBLEM
━━━━━━━━━━━━━━━━━━━━

A10 — No Autonomous Debugging

Current issue:
The AI can generate code and start servers, but when runtime errors happen, the system cannot autonomously:
- inspect logs
- identify root causes
- patch files
- retry execution
- verify fixes
- recover automatically

Currently manual fixes are required.

This prevents true Replit/Cursor-level autonomous coding.

━━━━━━━━━━━━━━━━━━━━
YOUR GOAL
━━━━━━━━━━━━━━━━━━━━

Design and implement a REAL autonomous debugging + self-healing workflow.

The system must:
1. Detect runtime/build errors
2. Read logs automatically
3. Identify failing files
4. Generate fixes
5. Apply patches
6. Restart runtime
7. Verify recovery
8. Retry intelligently
9. Escalate only after repeated failures

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Scan ALL related systems:

Frontend:
- chat
- console
- preview
- file explorer
- runtime hooks
- SSE handling

Backend:
- tool-loop agent
- runtime system
- process managers
- preview proxy
- event bus
- shell tools
- file tools
- server lifecycle tools
- orchestrators
- pipeline system
- agent memory
- retry handling
- cancellation handling

━━━━━━━━━━━━━━━━━━━━
FIND THESE PROBLEMS
━━━━━━━━━━━━━━━━━━━━

1. Missing runtime observation loop
2. Missing automatic crash detection
3. Missing log analysis
4. Missing file-error correlation
5. Missing retry orchestration
6. Missing patch verification
7. Missing rollback logic
8. Missing execution checkpoints
9. Missing recovery lifecycle
10. Missing autonomous debugging memory

━━━━━━━━━━━━━━━━━━━━
IMPORTANT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

STRICT RULES:

- Use HIGH COHESION
- Use LOW COUPLING
- Clear folder structure
- Modular architecture
- No god classes
- No giant files
- MAX 250 lines per file
- Single responsibility per module
- Reusable services
- Strong typing
- Clean interfaces
- Avoid circular dependencies
- Avoid recursive orchestration
- No duplicated runtime registries
- No duplicated event systems

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create a new autonomous debugging architecture:

server/
  agents/
    autonomous-debug/
      core/
      analyzers/
      patchers/
      recovery/
      verification/
      memory/
      types/
      events/

━━━━━━━━━━━━━━━━━━━━
SYSTEM REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Implement:

1. Runtime Error Observer
- watches runtime logs
- watches process crashes
- watches build failures

2. Error Analyzer
- extracts stack traces
- detects failing files
- detects failing packages
- identifies syntax/runtime/import errors

3. Recovery Planner
- decides:
  - retry
  - patch
  - rollback
  - reinstall packages
  - restart server

4. Autonomous Patch Engine
- generates patches
- applies file diffs safely
- validates patch before apply

5. Verification Engine
- restart runtime
- verify preview availability
- verify logs cleaned
- verify process healthy

6. Recovery Memory
- track:
  - previous failures
  - retries
  - failed fixes
  - successful fixes

7. Escalation System
- if retries exceeded:
  - emit agent.escalation
  - request human intervention

━━━━━━━━━━━━━━━━━━━━
SCAN FOR EXISTING FILES
━━━━━━━━━━━━━━━━━━━━

Find and audit:
- server lifecycle tools
- runtime managers
- preview runtime
- process maps
- shell execution
- console streaming
- agent loops
- tool orchestrator
- retry handlers
- error boundaries

Reuse existing code where possible.
DO NOT duplicate systems unnecessarily.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIXES
━━━━━━━━━━━━━━━━━━━━

Fix:
- missing runtime self-healing
- missing crash recovery
- missing intelligent retries
- missing autonomous patch loop
- missing verification loop
- missing rollback checkpoints

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root Cause Analysis
2. Current Failure Flow
3. Missing Autonomous Components
4. Exact Architectural Problems
5. File-Level Problems
6. Runtime Failure Lifecycle
7. Correct Self-Healing Flow
8. New Folder Structure
9. New Modules
10. New Interfaces
11. Event Lifecycle
12. Retry Strategy
13. Verification Strategy
14. Rollback Strategy
15. Step-by-Step Refactor Plan
16. Exact Files To Modify
17. Exact New Files To Create
18. Dependency Graph
19. Runtime Sequence Diagram
20. Production-Grade Final Architecture

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

- Keep every file under 250 lines
- Split logic cleanly
- No giant orchestrators
- No nested monoliths
- No duplicated registries
- No duplicate process maps
- No duplicate SSE systems

Build a REAL self-healing autonomous debugging system.
Act like you're engineering the debugging core of Replit Agent or Cursor.