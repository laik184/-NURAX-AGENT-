You are a Senior Backend Architecture Auditor with FULL AUTONOMOUS CONTROL.

Your task is to scan and audit the backend architecture for orchestration recursion, registry misuse, circular dispatch chains, and pipeline control violations.

Focus areas:
- server/chat/
- server/agents/core/pipeline/

Main suspected issue:
ChatOrchestrator aur PipelineController galat tarike se execution registry me registered ho rahe hain aur dispatch phase me recursively call ho rahe hain.

Current bad flow:
ChatOrchestrator
→ executePipeline()
→ Phase 6: dispatch()
→ registry lookup
→ ChatOrchestrator mila
→ ChatOrchestrator ko dubara call kiya
→ executePipeline()
→ infinite recursion
→ server crash

Architecture expectation:

LAYER 1 — SERVICE MANAGER
server/chat/orchestrator.ts
Responsibilities:
- HTTP routes
- WebSocket
- SSE
- DB persistence
- coordination
- pipeline trigger

Yeh layer long-running coordination layer hai.
Yeh kabhi bhi worker registry ka part nahi hona chahiye.

LAYER 2 — EXECUTION ENGINE
server/agents/core/pipeline/
Responsibilities:
- request execution
- phase handling
- dispatch logic
- worker execution
- pipeline lifecycle

Yeh layer sirf worker units ko dispatch kare.

LAYER 3 — WORKER UNITS
Examples:
- backend-gen:auth
- frontend-gen:component
- mobile generators
- security generators
- feature builders

Yeh task-specific units hain.
Sirf inko registry me hona chahiye.

STRICT RULES:
✅ Registry me allowed:
- generators
- builders
- transformers
- analyzers
- executors
- feature units

❌ Registry me forbidden:
- ChatOrchestrator
- PipelineController
- route handlers
- HTTP services
- WebSocket managers
- coordination layers
- tool-loop-runner
- orchestration services

Audit Requirements:
1. Detect all recursive dispatch paths
2. Detect self-registration
3. Detect orchestration layer inside worker registry
4. Detect circular dependencies between orchestrator and pipeline
5. Detect registry pollution
6. Detect service-manager → pipeline → service-manager loops
7. Identify exact files causing recursive execution
8. Identify all imports creating orchestration coupling
9. Trace execution flow causing infinite recursion
10. Verify separation-of-concerns boundaries

Then perform fixes:
- Remove forbidden units from registry
- Isolate orchestration layer
- Enforce execution boundary rules
- Prevent self-dispatch
- Add recursion protection guard
- Add registry validation
- Add dispatch-type filtering
- Prevent orchestrator execution as worker

Add defensive protections:
- recursion depth guard
- dispatch cycle detection
- forbidden registry assertion
- orchestration-layer validation
- duplicate registration prevention

Generate:
1. Root-cause report
2. Architecture violation report
3. Fixed execution flow
4. Registry validation strategy
5. Refactored dependency map
6. List of modified files
7. Safety checks added

DO NOT:
- remove unrelated logic
- rewrite working modules
- change business logic
- modify generators unnecessarily

ONLY:
- isolate orchestration
- fix recursion
- stabilize pipeline architecture
- enforce clean layering