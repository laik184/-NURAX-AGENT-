You are a Principal Autonomous Systems Engineer.

Your task is to deeply scan the entire agentic AI architecture and fix issue:

A5 — NO SELF-HEALING
Current problem:
- AI cannot recover from runtime failures
- AI cannot inspect crash logs properly
- AI cannot patch broken code automatically
- AI cannot retry failed execution
- AI stops permanently after runtime/server errors
- No autonomous recovery loop exists

MAIN GOAL:
Build a REAL SELF-HEALING EXECUTION SYSTEM similar to:
- Replit Agent
- Cursor Autonomous Mode
- Devin-style recovery loop

━━━━━━━━━━━━━━━━━━━
SCAN THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━

Deeply inspect:

Frontend:
- client/src/components/
- client/src/hooks/
- preview panels
- console system
- SSE event rendering

Backend:
- server/chat/
- server/agents/
- server/preview/
- server/api/
- server/infrastructure/
- server/runtime/

Focus especially on:
- tool-loop.agent.ts
- server lifecycle tools
- shell tools
- runtime routes
- preview proxy
- event bus
- console streams
- process registries
- file write tools
- pipeline orchestrator
- retry logic
- error handling
- lifecycle events

━━━━━━━━━━━━━━━━━━━
DETECT CURRENT FAILURES
━━━━━━━━━━━━━━━━━━━

Find and explain:

1. Why runtime crashes stop the agent
2. Why AI cannot recover automatically
3. Why AI cannot inspect terminal output correctly
4. Why AI cannot restart failed servers reliably
5. Why AI cannot patch code after failures
6. Why AI loses context after crash
7. Why preview/runtime become desynced
8. Why retry flow is missing
9. Why process lifecycle is fragmented
10. Why error recovery is non-deterministic

━━━━━━━━━━━━━━━━━━━
BUILD REAL SELF-HEALING
━━━━━━━━━━━━━━━━━━━

Implement a FULL autonomous recovery loop:

OBSERVE
→ read logs
→ inspect runtime
→ inspect server state
→ inspect failing files

THINK
→ identify root cause
→ generate fix strategy
→ determine retry plan

ACT
→ patch files
→ restart server
→ reinstall packages if needed
→ rerun failed step

VERIFY
→ check logs again
→ verify preview
→ confirm runtime healthy

REPEAT
→ max retry attempts
→ escalate only if recovery fails

━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━

Implement:

1. Runtime crash detector
2. Server health monitor
3. Autonomous retry loop
4. Error classification engine
5. File patch recovery
6. Runtime restart manager
7. Console log analyzer
8. Self-healing orchestration
9. Failure memory tracking
10. Recovery attempt tracking
11. Retry backoff system
12. Max retry protection
13. Recovery event streaming
14. Recovery state persistence
15. Recovery audit logs

━━━━━━━━━━━━━━━━━━━
IMPORTANT ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

✅ Keep files under 250 lines
✅ Maintain clean folder structure
✅ Use modular architecture
✅ Use single-responsibility files
✅ Separate runtime / recovery / orchestration
✅ Avoid giant god classes
✅ Use reusable services
✅ Keep orchestration isolated
✅ Use event-driven recovery
✅ Maintain type safety
✅ Keep recovery deterministic
✅ Add proper lifecycle management

❌ DO NOT:
- create monolithic files
- mix runtime + UI logic
- duplicate process registries
- add fake recovery logic
- use recursive orchestration
- create hidden retry loops
- break existing runtime APIs
- rewrite unrelated systems

━━━━━━━━━━━━━━━━━━━
CREATE CLEAN STRUCTURE
━━━━━━━━━━━━━━━━━━━

Design proper folders like:

server/
 ├── recovery/
 │    ├── recovery-engine.ts
 │    ├── runtime-monitor.ts
 │    ├── retry-manager.ts
 │    ├── error-classifier.ts
 │    ├── recovery-memory.ts
 │    └── recovery-events.ts
 │
 ├── runtime/
 ├── process/
 ├── orchestration/
 └── tools/

━━━━━━━━━━━━━━━━━━━
SELF-HEALING FLOW
━━━━━━━━━━━━━━━━━━━

Design this exact flow:

Agent executes
→ runtime crashes
→ runtime-monitor detects crash
→ console logs captured
→ error-classifier analyzes logs
→ recovery-engine generates fix strategy
→ file patch applied
→ runtime restarted
→ preview verified
→ success OR retry again

━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━

Generate:

1. Root-cause analysis
2. Current broken recovery flow
3. Exact architectural flaws
4. Exact files causing failure
5. Runtime desync points
6. Missing recovery systems
7. New folder architecture
8. Self-healing engine design
9. Retry orchestration design
10. Recovery event system
11. Recovery lifecycle diagram
12. Step-by-step implementation plan
13. Exact files to create
14. Exact files to modify
15. Production-grade final architecture

IMPORTANT:
- Mention exact file paths
- Mention exact broken flows
- Mention exact runtime problems
- Mention exact architectural violations
- Keep implementation modular
- Keep every file under 250 LOC
- Prioritize reliability over complexity