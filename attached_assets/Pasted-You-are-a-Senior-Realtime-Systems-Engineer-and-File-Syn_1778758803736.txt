You are a Senior Realtime Systems Engineer and File Synchronization Architect.

Your task is to deeply scan the entire codebase and FIX the following critical architecture bug:

━━━━━━━━━━━━━━━━━━━━
BUG ID: P5
━━━━━━━━━━━━━━━━━━━━

Problem:
file.change events are missing when the AI agent writes files.

Impact:
- File Explorer becomes stale
- UI does not auto-refresh
- Agent-generated files appear delayed or invisible
- Preview/runtime state becomes desynchronized
- User thinks AI is not working
- Realtime developer experience breaks

Current suspected behavior:
- Agent tools use fs.writeFile directly
- file.change events are only emitted by chokidar watcher
- Agent writes bypass the watcher event system
- Frontend SSE subscribers never receive realtime updates

━━━━━━━━━━━━━━━━━━━━
MAIN OBJECTIVE
━━━━━━━━━━━━━━━━━━━━

Create a REAL unified realtime file synchronization flow.

When ANY file changes:
- AI tool write
- AI replace
- AI delete
- manual editor save
- shell-generated file
- package install
- git operation
- runtime-generated file

The system MUST emit:
- file.change event
- SSE update
- File Explorer refresh
- Preview refresh trigger

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Deeply scan and analyze:

Frontend:
- file explorer
- editor state
- dirty files logic
- SSE listeners
- refresh logic
- realtime hooks
- file tree caching

Backend:
- file_write tool
- file_replace tool
- file_delete tool
- sandbox file operations
- chokidar watchers
- SSE streams
- event bus
- runtime file generation
- package install side effects
- git operations

━━━━━━━━━━━━━━━━━━━━
FIND:
━━━━━━━━━━━━━━━━━━━━

1. Exact locations where fs.writeFile is used
2. All places bypassing file.change events
3. Missing event emissions
4. Broken SSE propagation
5. Missing frontend refresh handlers
6. Duplicate file watcher systems
7. Realtime desync risks
8. Event bus inconsistencies
9. Preview refresh gaps
10. File explorer stale-state causes

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create ONE unified file event pipeline:

ANY FILE CHANGE
    ↓
Central File Event Service
    ↓
bus.emit("file.change")
    ↓
SSE/WebSocket propagation
    ↓
Frontend state sync
    ↓
File Explorer refresh
    ↓
Preview refresh

━━━━━━━━━━━━━━━━━━━━
STRICT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

1. DO NOT create duplicate watcher systems
2. DO NOT create polling loops
3. DO NOT add unnecessary abstractions
4. DO NOT break existing file tools
5. DO NOT exceed 250 lines per file
6. Keep functions small and modular
7. Keep folder structure clean
8. Use production-grade naming
9. Use centralized event emission
10. Preserve sandbox isolation
11. Prevent event spam/debounce storms
12. Avoid memory leaks
13. Avoid duplicate SSE emissions

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIXES
━━━━━━━━━━━━━━━━━━━━

Implement:

1. Unified File Event Emitter
2. Shared file-change utility
3. Automatic event emission on:
   - write
   - replace
   - delete
   - rename
   - mkdir
4. SSE propagation fixes
5. Frontend realtime refresh fix
6. Preview refresh trigger
7. Event deduplication
8. Debounced refresh handling
9. Realtime state synchronization

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FILES TO CHECK
━━━━━━━━━━━━━━━━━━━━

Likely areas:
- server/agents/tools/categories/
- server/chat/streams/
- server/infrastructure/events/
- server/infrastructure/sandbox/
- client/src/components/
- client/src/hooks/
- client/src/context/

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Exact broken flow
3. Exact files causing bug
4. Exact lines causing stale explorer
5. Unified architecture plan
6. Refactor strategy
7. Modified file list
8. New utility/service files
9. Event flow diagram
10. Memory safety checks
11. SSE synchronization strategy
12. Preview synchronization strategy
13. Final production-grade implementation

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

This is NOT just a frontend refresh bug.

This is a SYSTEM-WIDE REALTIME ARCHITECTURE PROBLEM.

Fix it like a production-grade Replit/Cursor realtime sync system.

Use evidence-based analysis only.
Mention exact file names and exact lines.
Keep architecture scalable and clean.