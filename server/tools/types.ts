/**
 * server/tools/types.ts
 *
 * Convenience re-export so category files can use the short path `../types.ts`.
 */
export type { Tool, ToolContext, ToolResult, ToolDef } from "./registry/tool-types.ts";
