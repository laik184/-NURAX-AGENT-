/**
 * server/tools/registry.ts
 *
 * Backward-compat re-export for any code that imports from
 * 'server/tools/registry.ts' directly.
 *
 * The canonical import path is now 'server/tools/index.ts'.
 */

export * from "./index.ts";
